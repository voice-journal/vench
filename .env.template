# ==========================================
# 1. 데이터베이스 설정 (MySQL)
# ==========================================
# Docker 컨테이너 내부 및 로컬 테스트용
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=

# SQLAlchemy 연결 주소
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/vench?charset=utf8mb4

# ==========================================
# 2. 보안 설정 (JWT Auth)
# ==========================================
# 토큰 서명에 사용할 비밀키 (배포 시 변경 필수!)
JWT_SECRET_KEY=
# 액세스 토큰 만료 시간 (분 단위)
ACCESS_TOKEN_EXPIRE_MINUTES=

# ==========================================
# 3. 서비스 연결 설정 (Frontend <-> Backend)
# ==========================================
# Streamlit이 백엔드 API를 호출할 주소
# Docker Compose 내부 통신용: http://backend:8000
# 로컬 실행용: http://localhost:8000
BACKEND_URL=

# ==========================================
# 4. 모니터링 설정 (Grafana)
# ==========================================
# Grafana 관리자 초기 비밀번호
GF_SECURITY_ADMIN_PASSWORD=